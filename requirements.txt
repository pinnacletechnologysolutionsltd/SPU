# SPU-13 Sovereign Engine — Python dependencies
# Install into the project venv:
#   python3 -m venv venv
#   source venv/bin/activate
#   pip install -r requirements.txt
#
# The venv is gitignored. Always activate before running:
#   source venv/bin/activate
#   python3 run_all_tests.py
#   python3 software/tools/spu_terminal.py

# Terminal UI (live multi-panel layout for SPU Terminal)
rich>=13.0

# Hardware serial bridge (RP2040 USB CDC → spu_vm.py)
pyserial>=3.5

# Test runner / emulator (stdlib only — no extra deps needed)
# spu_vm.py uses only stdlib; listed here for documentation
# pytest>=7.0   # optional: uncomment if you prefer pytest over run_all_tests.py
