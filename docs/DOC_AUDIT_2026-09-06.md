# Documentation & Repository Audit — 2026-09-06

**Status:** findings only. Nothing has been changed as a result of this audit.
**Method:** mechanical sweeps via `tools/doc_audit.py`, plus judgement-level
review of the top-level documents. Every mechanical finding is reproducible.

## How to reproduce

```bash
python3 tools/doc_audit.py --actual-pass 226      # all checks
python3 tools/doc_audit.py --check claims         # one class
python3 tools/doc_audit.py --json                 # machine-readable
```

`--actual-pass 226` is today's measured suite total (`bash tools/verify_repo.sh`,
2026-09-06). Re-measure before quoting it.

## The structural finding, which explains most of the rest

`tools/verify_repo.sh`'s evidence-citation gate is **diff-scoped by design** —
the 2026-08-19 governance rewrite made it check only lines changed since HEAD,
so that pre-existing documents need not be cleared to pass. That was a
deliberate and reasonable decision.

**Its consequence was never stated: the existing corpus has never been
audited by it.** Everything below in the "uncited claims" class predates the
gate or was never touched by a commit that would trigger it. The gate is not
broken; it has simply never looked here.

---

## P0 — public-facing, and wrong today

### 1. `README.md:3–7` — the headline describes a shelved product

The first sentence a reader sees:

> *"Its current proven product path is an edge-classification sidecar: labeled
> CSV in, checksummed rational SOM map out..."*

The SOM/anomaly-detection wedge was **shelved 2026-09-03**. The README also
carries "This SOM path is the current SPU-13 platform wedge", "the separate
commercial direction is now the smaller SPU-4 Sentinel", "physical INA226
acquisition remains the next sensor bench step" (shelved), and an
"Artix-7 200T / Kintex-class **funding target**".

**And the actual current direction is absent.** Three silicon milestones in
three consecutive days — §3.8 first video (09-04), §3.9 first rasterized
geometry (09-05), §3.10 first depth-resolved image (09-06) — appear nowhere in
the README.

### 2. `README.md:154` — the reproduction surface contradicts itself

Inside the section titled **"Check it yourself (no trust required)"**:

```
python3 run_all_tests.py     # currently prints "Total PASS: 193"
```

It prints **226**. A reader following the README's own no-trust-required
instructions gets a different number than promised, in the one section whose
entire purpose is that they should not have to trust it. Same at line 202.

### 3. `README.md:41` — "There are currently seven" `[NO ENTRY]` claims

`docs/CURRENT_STATUS.md` carries **two** substantive `[NO ENTRY]` markers.

---

## P1 — 49 assertive silicon claims with no evidence reference

Method: an assertive `silicon-verified` / `silicon-proven` / `proven in
silicon` statement with **no** `hardware_evidence` or `§3.x` reference within
±10 lines. Negated forms ("does not claim a silicon-verified balancer") are
excluded, as are handovers, archives, blog and the ledger itself.

| file | count | file cites evidence at all? |
|---|---:|---|
| `knowledge/SPU13_ARCHITECTURE.md` | 11 | — |
| `docs/ZPHI_KARATSUBA_INTEGRATION_PLAN.md` | 4 | — |
| `docs/SPU13_IDENTITY_AND_BOUNDARIES.md` | 3 | **0 times** |
| `docs/TENSEGRITY_BALANCER_HANDOVER_2026-07-15.md` | 3 | — |
| `docs/som_temporal_classification.md` | 3 | — |
| `docs/spu13_isa_spec.md` | 3 | — |
| `docs/QUADRAY_FOR_PROGRAMMERS.md` | 1 | **0 times** |
| `docs/ROTC_THIRDS_EXACTNESS_FIX.md` | 1 | **0 times** |
| `README.md` | 1 | 4 times |

The three files that never reference `hardware_evidence.md` at all are the
ones to read first: a claim there cannot be traced by a reader.

**This is a backlog, not necessarily 49 errors.** A spec may cite its evidence
in a distant section the ±10-line window cannot see. AGENTS.md §3.3 is
explicit about the alternative wordings — `testbench-verified`,
`simulation-only`, `unmeasured` — so the fix for a genuinely unbacked claim is
usually a downgrade, not a deletion.

---

## P2 — mechanical, low judgement

**Stale suite totals (5).** Measured actual 226:

```
README.md:154                          claims 193
README.md:202                          claims 193
docs/CURRENT_STATUS.md:114             claims 173   (already marked [SUPERSEDED])
docs/FIRST_HOUR.md:44                  claims 173
spu_strategy/tranche_plan_2026-08-12.md:55  claims 192
```

`FIRST_HOUR.md` matters most after the README — it is the onboarding path.

**Broken internal links (12).** Seven are in
`photonics/PHOTONICS_RESEARCH_INDEX.md` and point into `spu_strategy/`, which
is **gitignored** (`.gitignore:112`) and was **permanently lost to `rm -rf` on
2026-08-17**. Those targets are not recoverable; the index should say so
rather than link to them. Two in `knowledge/ISA_QUICKSTART.md` and one in
`knowledge/isa_reference.md` are **path bugs, not missing files** — `../X.md`
from `knowledge/` resolves to the repo root instead of `docs/`. Two in
`STSC_SSAM_MULTI_AGENT_NOTES.md` are C++ code fragments the regex mistook for
links; ignore them.

---

## P3 — structural, needs a decision rather than a fix

**105 of 212 tracked markdown files are linked from no other tracked markdown
file.** Half the documentation is unreachable by navigation. Orphaned is not
automatically wrong — bench procedures and specs are legitimately standalone,
and some are reached from code comments or handovers. But at 50% there is no
navigable structure, and it is the documentation analogue of the
zero-instantiation problem `tools/rtl_instantiation_audit.py` was built for on
the RTL side.

**Suggested split, not yet done:** an index of what is current, an explicit
archive for what is superseded, and deletion for what is wrong. The
distinction matters — this project keeps negative results **by policy**
(AGENTS.md), so "superseded" must not be conflated with "delete".

---

## What this audit did NOT check

Stated so the gaps are known rather than assumed covered:

- **Prose accuracy.** Only claim *form* is checked, never whether a cited
  section actually supports the claim made.
- **Code/doc agreement.** Whether a documented port, opcode or file path still
  matches the RTL.
- **Cross-document contradiction.** Two docs asserting incompatible things
  will both pass every check here.
- **The `spu_strategy/` corpus**, which is gitignored and so invisible to
  `git ls-files`.
- **Blog and papers**, excluded from the claims sweep.
  `docs/blog/zero_drift_phi_arithmetic.md` is separately known to claim a
  non-existent arXiv paper, cite `github.com/spu13` which never existed, quote
  an unsourced 200 MHz figure, and assert "silicon-proven SOM classification"
  post-shelving (09-05 §6 recommended cutting it; no decision taken).

## For the reviewers

Highest value if you disagree with any of these:

1. **Is the P1 count real?** The ±10-line window is arbitrary. A wider window,
   or per-file rather than per-claim citation, may be the fairer test.
2. **Which P1 hits are genuine unbacked claims** versus claims whose evidence
   is simply cited elsewhere in the file? That needs reading, not regex.
3. **Is 105 orphans a problem or a non-finding?** I lean "structural problem",
   but a documentation set can legitimately be a reference library rather than
   a navigable tree.
4. **Anything in "did NOT check" that should have been.** Cross-document
   contradiction is the one I would most expect to be hiding real errors.
